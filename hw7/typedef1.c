#include <stdio.h>

typedef int Integer;

int main() {
    	Integer a = 10, b = 20;
    	Integer sum = a + b;

    	printf("a = %d, b = %d\n", a, b);
    	printf("Sum = %d\n", sum);

    	return 0;
}

